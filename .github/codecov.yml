coverage:
  status:
    project:
      src:
        target: auto
        paths: "src/"
      unittests:  # declare a new status context "unittests"
        target: 100%  # we always want 100% coverage here
        paths: "test/unit_tests/"  # only include coverage in "test/unit_tests" folder
