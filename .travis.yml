sudo : false

notifications:
  email: false

language: Fortran

compiler:
    - gfortran

install:
    - ./tools/build.sh

script:
    - ./contrib/utilities/check_indentation.sh

branches:
  only:
- master
